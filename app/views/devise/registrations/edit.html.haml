%h1
  Edit #{resource_name.to_s.humanize}
= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
  - if resource.errors.full_messages.present?
    .alert.alert-info.alert-dismissable
      = devise_error_messages!
  .field.form-group
    %h4
      =f.label :name
    = f.text_field :name, autofocus: true, class: "form-control"
  .field.form-group
    %h4
      = f.label :email
    = f.email_field :email, class: "form-control"
  - if devise_mapping.confirmable? && resource.pending_reconfirmation?
    .field.form-group
      Currently waiting confirmation for: #{resource.unconfirmed_email}
  .field.form-group
    %h4
      = f.label :password
    %i (leave blank if you don't want to change it)
    = f.password_field :password, autocomplete: "off", class: "form-control"
  .field.form-group
    %h4
      = f.label :password_confirmation
    = f.password_field :password_confirmation, autocomplete: "off", class: "form-control"
  .field.form-group
    %h4
      = f.label :current_password
    %i (we need your current password to confirm your changes)
    = f.password_field :current_password, autocomplete: "off", class: "form-control"
  .actions
    = f.submit "Update", class: 'btn btn-primary btn-lg btn-block'
%h3 Cancel my account
%p
  #{button_to "Cancel my account", registration_path(resource_name), class: 'btn btn-primary btn-lg btn-block', data: { confirm: "Are you sure?" }, method: :delete}
-# %h4
-#   = link_to "Back", :back
